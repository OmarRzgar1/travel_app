import 'package:flutter/material.dart';
import '../models/trip.dart';

class FavoritesScreen extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('you dont have a favorite travels'),
    );
  }
}
